# branch?

# 사전적 의미
나뭇가지라는 의미

# master branch
깃으로 버전관리를 시작하면 기본적으로 master라는 브랜치가 생성됨
마스터 브랜치는 사용자가 커밋할 때마다 최신 커밋을 카르킴
브랜치는 커밋을 가르키는 포인터와 유사하다 할 수 있음

# branch / marge
새로운 브랜치를 만드는 것을 분기(branch)라고 함
분기했던 브랜치를 마스터 브랜치에 합하는 것을 병합(marge)라고 함


